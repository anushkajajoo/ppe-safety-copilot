Datasets live here but are NOT committed to Git (see .gitignore).
- datasets/ppe4/  <- created by: python -m training.prepare_sh17 --src <SH17 folder>
Record the SH17 download date and source URL in docs/decisions.md.
